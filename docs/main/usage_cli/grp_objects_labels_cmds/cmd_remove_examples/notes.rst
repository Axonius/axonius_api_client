.. include:: /main/.special.rst

Notes
==========================================

.. note::

   This example works the same for both the ``devices labels remove`` and
   ``users labels remove`` commands.

.. note::

   This command can take a while because the REST API can only remove tags from 200 assets
   at a time.
